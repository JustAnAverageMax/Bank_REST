spring:
  liquibase:
    user: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    contexts: test
  datasource:
    url: ${DATASOURCE_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        generate_statistics: true